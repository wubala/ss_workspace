# 2026-03-30

- 爸爸确认：命令行里的 `openclaw` 补全不出来，原因大概率是 Oh My Zsh / shell completion 没接好，不是 `openclaw` 命令本身坏了。
- 已检查 `~/.zshrc`，在 `source $ZSH/oh-my-zsh.sh` 后补了一段最小化的 OpenClaw completion 加载，只在 `~/.openclaw/completions/openclaw.zsh` 存在时 `source`，不改动其他插件、别名、p10k、zoxide、fzf 等配置。
- 已通过 `openclaw completion --shell zsh --write-state` 生成补全脚本到 `~/.openclaw/completions/openclaw.zsh`，并确认脚本里有 `compdef _openclaw_root_completion openclaw`。
- 已帮爸爸把这次对话整理记录到 `archives/notesPr.md`，归档主题是 OpenClaw 补全配置与最小化修改。
- 爸爸澄清：以后“记住”这类请求时，希望按内容判断是记到当天的临时记录还是长期记忆；按日期保存的文件属于短期/原始记录，`MEMORY.md` 才是长期记忆。
- 爸爸最新要求：如果是今天的对话内容，也可以先按当天记录保存，再由我判断是否值得提炼进长期记忆。
