# 2026-03-29

- 爸爸明确了 `duihua-guidang` 的归档格式要求：`## 内容` 下面不要固定写“### 正文”，而是要把内容各项直接写成三级目录，格式为 `### 1、xxx`、`### 2、xxx`、`### 3、xxx` 依次编号。对应脚本和 skill 说明已调整。
- `archives/notesPr.md` 被按爸爸要求清空过，随后重新用新格式记录了两条内容：`item日常操作（插件）` 和 `头像定位过程`。
- 帮爸爸把 `/Users/a1-6/Desktop/2.jpeg` 和 `/Users/a1-6/Desktop/3.jpeg` 中的卷子区域裁出，并生成合并 PDF：`/Users/a1-6/Desktop/exam_merged.pdf`。
- 爸爸当前偏好的 iTerm2 风格是深色系里的“墨绿黑”背景，不要纯黑；同时原本过暗的黑色前景需要提亮，否则在深绿背景下看不清。
- 爸爸的终端里出现“小方块问号”图标问题，判断为 Nerd Font/终端字体兼容性问题；已开始安装兼容字体 `MesloLGS Nerd Font`，安装进程在本次会话结束前仍未确认完成。
- SkillHub CLI 已安装；已成功安装 `github`、`self-improving-agent`、`ontology`、`find-skills`、`summarize`、`self-improving`、`memory-setup`、`ui-audit`、`skill-vetter` 等 skills。`clawpanel` 不是有效 slug，安装时返回 404。
- 记忆机制：长期记忆通常需要把重要信息明确写入；不主动提的内容一般不会长期保存，但会保留当前会话上下文。