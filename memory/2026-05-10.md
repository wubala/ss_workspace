# 2026-05-10

## 今日记录

- 今晚执行了例行“夜间记忆整理并同步 GitHub”任务。
- 检查到 `memory/2026-05-09.md` 与 `memory/2026-05-10.md` 之前都不存在；本次已创建今天的 daily memory 文件。昨天的 daily memory 仍缺失，暂不伪造补写。
- 本次优先依据工作区本地文件与已落盘改动整理记忆；未找到可直接回放今天详细对话内容的本地会话日志，因此不臆造“今天聊过什么”。
- 当前工作区里确认到一条新增且已写入 `.learnings/ERRORS.md` 的经验：`sessions_send` 调用时，`sessionKey` 和 `label` 不能同时传，必须二选一，否则会报错 `Provide either sessionKey or label (not both).`
- 该经验更适合保留在 `.learnings/ERRORS.md` 作为工具使用层面的错误记录；本次未发现必须新增到 `MEMORY.md` 的、对爸爸长期稳定有价值的新个人偏好或新长期设定。
- 本次 Git 同步目标仓库为 `/Users/a1-6/.openclaw/workspace`；同步前确认该目录是 git 仓库，且存在未提交改动：`.learnings/ERRORS.md`。
