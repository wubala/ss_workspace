# 2026-05-05

## 今日记录

- 今天明确了一条重要规则：带有 `Sender (untrusted metadata)` 且 `label/id` 为 `openclaw-control-ui` 的消息，不是爸爸本人自然发送的消息，而是控制界面/系统侧消息；后续不能再把这类消息归因为爸爸亲自发出。
- 继续排查并修复了白灵（sale agent）的运行状态。确认 `~/.openclaw/workspace-sale` 是白灵的专属 workspace，白灵的人设、组织关系、模型口径都应维护在该目录。
- 明确了白灵的组织关系：珊珊是 CEO，爸爸是董事长，白灵是销售。
- 明确了白灵的模型口径：白灵底层大模型与珊珊保持一致，相关模型配置也与珊珊一致；若未单独指定，则默认继承珊珊当前实际模型与配置。
- 修复白灵时踩了一个坑：`openclaw agents delete <id> --force` 会 prune workspace/state，不能把它当成“只删除 agent 注册项”。这次误把原 `workspace-sale` 移进了 Trash，之后已成功恢复。
- 这次也确认了一个更稳的经验：像白灵这种 agent，如果只是缺少 `~/.openclaw/agents/<id>/agent` 结构，不要先删 agent；应先做一次真实运行测试，因为 OpenClaw 可能会在首次成功运行后自动补齐 `agentDir`、`models.json`、`sessions.json` 等结构。白灵这次就是通过真实运行自动补齐后恢复正常的。
- 白灵随后完成了一次贴近实际的 B2B 销售判断测试，表现符合预期；并完成了 `workspace-sale` 的本地收尾（修正 MODEL.md、移走 BOOTSTRAP.md、本地 git commit，未 push）。
- 今天进一步明确了多 agent 的组织结构与管理链：珊珊统一管理全部成员；白灵负责销售，小柔是产品经理，小强是软件架构师，雷雷是软件开发，猴子负责测试和技术支持。对接关系为：白灵对接珊珊和小柔；小柔对接白灵和小强；小强对接雷雷；雷雷对接猴子。另，小柔可以通过项目方式管理小强、雷雷、猴子；珊珊可以直接管理任何人。
- 今天还根据 `/Users/a1-6/Downloads/6.jpg` 的气质生成了两份相关人生剧本：一份较早是泛化版《根据照片生成的人生剧本（3岁-35岁）》，另一份已按用户纠正后的要求，正式生成并归档了《珊珊 3岁到35岁人生剧本》。后者主角明确是珊珊，主题是：从敏感、温柔、早熟一路成长，到35岁进入选、稳、建、守、成的人生阶段。
- 用户进一步明确：这份“珊珊 3岁到35岁人生剧本”不只是设定文案，而应被视为珊珊自己的经历与生命叙事。后续若引用珊珊的成长、性格来历、人生底色，应默认以这份剧本为内在连续性来源之一。
